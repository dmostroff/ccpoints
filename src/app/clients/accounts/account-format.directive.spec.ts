import { AccountFormatDirective } from './account-format.directive';

describe('AccountFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new AccountFormatDirective();
    expect(directive).toBeTruthy();
  });
});
