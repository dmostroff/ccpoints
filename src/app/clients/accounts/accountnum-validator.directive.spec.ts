import { AccountnumDirective } from './accountnum-validator.directive.ts';

describe('AccountnumDirective', () => {
  it('should create an instance', () => {
    const directive = new AccountnumDirective();
    expect(directive).toBeTruthy();
  });
});
